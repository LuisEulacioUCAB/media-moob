query products($limit:Int, $offset:Int){
    products(limit:$limit, offset:$offset){
        id
        images
        title
        description
        price
        category{
            id
            name
        }
    }
}